apiVersion: oam.conure.io/v1alpha1
kind: Application
metadata:
  name: first-app
spec:
  components:
    - name: myservice
      type: service
      properties:
        image: "busybox"
        cmd:
          - sleep
          - "1000"