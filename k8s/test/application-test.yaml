apiVersion: oam.conure.io/v1alpha1
kind: Application
metadata:
  name: first-app
spec:
  components:
    - name: myservice
      type: service
      replicas: 3
      properties:
        image: "busybox"
        cmd:
          - sleep
          - "3000"